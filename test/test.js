var should = require('should');var config = require('../examples/sample_input.json');var swarmlicator = require('../index.js');it('should be able to initialize using sample_input', function (done) {  swarmlicator.setup(config);  done();});it('should be able to initialize using sample_input', function (done) {  try {    swarmlicator.setup({});    } catch (e) {    done();  }  });